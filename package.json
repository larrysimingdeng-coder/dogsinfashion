{
  "name": "dogsinfashion",
  "private": true,
  "scripts": {
    "dev": "concurrently -n fe,be -c blue,green \"cd frontend && npm run dev\" \"cd backend && npm run dev\"",
    "build": "cd frontend && npm run build && cd ../backend && npm run build",
    "dev:fe": "cd frontend && npm run dev",
    "dev:be": "cd backend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
